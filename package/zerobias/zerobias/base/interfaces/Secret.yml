description: "Secret"
links:
  models:
    # --- compliance features ---
    - f_shoc     # Secure handling of credentials
properties:
  - versions:
    field:
      description: "Secret versions"
      type: number
  # Links
  - container:
    linkTo: SecretsContainer
