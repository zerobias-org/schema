description: 'UUIDs of platform Vendors that issue this credential; may be empty while the issuer is pending moderation or absent from the Vendor catalog'
displayName: 'Issuer Vendor IDs'
type: string
multi: true
