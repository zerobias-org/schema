description: 'Date this VERIFICATION goes stale and the claim must be re-checked; null if the verification does not expire. Distinct from any domain expiry on the same class — insuranceCoverage.expiresAt is when the POLICY lapses, orgCredential/userCredential.expiresAt when the CREDENTIAL does.'
displayName: 'Verification Expires At'
type: date
