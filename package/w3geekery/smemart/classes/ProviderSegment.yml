description: "Org-scoped expertise junction: a provider Org's claimed capability in a Catalog Segment (Domain/Category). One row per (org, segment) claim with its own verification provenance (D-53). Segment name resolved from the Catalog on read (not denormalized). Distinct from OrgSegment (universal classification) — this is provider-capability."
extends:
  - Object
  - Verifiable
properties:
  - orgId:
    field: providerSegment.orgId
  - segmentId:
    field: providerSegment.segmentId
  - isPrimary:
    field: providerSegment.isPrimary
viewProperties:
  "Name":
    jsonata: name
    sort: name
  "Primary":
    jsonata: isPrimary
    sort: isPrimary
  "Verified":
    jsonata: verified
    sort: verified
